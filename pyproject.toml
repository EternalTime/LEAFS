[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pyLEAFS"
version = "0.1.0"
description = "Layered Environment with Agents Foraging Simulator"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Damian R Sowinski", email = "drsowinski@gmail.com" }]
requires-python = ">=3.8"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Physics",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "numpy>=1.17",
    "matplotlib>=3.3",
]

[project.optional-dependencies]
docs = [
    "sphinx",
    "sphinx-rtd-theme",
    "sphinx-copybutton",
]
test = [
    "pytest",
]

[project.urls]
Homepage = "https://github.com/EternalTime/LEAFS"
Documentation = "https://damiansowinski.com/LEAFS/"

[tool.setuptools.packages.find]
include = ["pyLEAFS*"]
